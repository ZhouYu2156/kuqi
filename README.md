# JK 知识站

Nuxt 4 站点：音乐试听、视频教程占位、会员与统一鉴权（JWT + HttpOnly Cookie）。

## 开发

```bash
pnpm install
pnpm dev
```

环境变量见 `nuxt.config` 中 `runtimeConfig`；数据库迁移在 `server/db/migrations/`。

## 构建

```bash
pnpm build
```
